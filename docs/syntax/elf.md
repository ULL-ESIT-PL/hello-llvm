# ELF

ELF is the Executable and Linkable Format.

It is the standard binary file format used on most Unix-like systems (especially Linux) for:

- executables
- shared libraries (`.so`)
- object files (`.o`)
- core dumps

It defines how machine code, symbols, relocations, debug info, and metadata are laid out in a binary so tools like the linker and loader can understand them.